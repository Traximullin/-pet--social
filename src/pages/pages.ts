export enum EPage {
    HOME = "/",
    LOGIN = "/login",
    REGISTATION = "/registration",
    PROFILE = "/profile",
    TREE = "/tree",
    CONTENT = "/content",
    CREATE_CONTENT = "/content/create",
}
